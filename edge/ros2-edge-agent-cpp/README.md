# ROS2 Edge Agent

Placeholder for the future C++/rclcpp edge agent.

This component should consume the same command and event protocol as `apps/cloud-edge-simulator` and connect outbound to the Fleet Platform.

