# Cloud Kubernetes Reference

Placeholder for production-reference cloud control-plane manifests.

GitOps deploys software versions. The Fleet Platform dispatches missions.

For a short-lived AWS/Kubernetes portfolio recording, use the
[AWS/Kubernetes demo runbook](../../../docs/aws-kubernetes-demo-runbook.md). That
runbook covers evidence capture and teardown without treating this placeholder
as a deployable production stack.
